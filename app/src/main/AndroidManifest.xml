<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ina97.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".diary_read" android:label="@string/app_name">
        </activity>

        <activity android:name=".diary_write" android:label="@string/app_name">
        </activity>

        <activity android:name=".pic_list" android:label="@string/app_name">
        </activity>

        <activity android:name=".letter_box" android:label="@string/app_name">
        </activity>

        <activity android:name=".letter_write" android:label="@string/app_name">
        </activity>

        <activity android:name=".letter_read" android:label="@string/app_name">
        </activity>

        <activity android:name=".setting" android:label="@string/app_name">
        </activity>
    </application>

</manifest>